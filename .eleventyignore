# https://www.11ty.dev/docs/ignores/

# helper files
src/_11ty

# files that will directly be copied to the dist directory during build
src/_copied_directly

# scripts and styles
src/scripts
src/styles
